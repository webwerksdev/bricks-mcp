<?xml version="1.0"?>
<ruleset name="Bricks MCP">
	<description>PHP CodeSniffer configuration for Bricks MCP plugin.</description>

	<!-- Scan these files -->
	<file>.</file>

	<!-- Exclude paths -->
	<exclude-pattern>/vendor/*</exclude-pattern>
	<exclude-pattern>/node_modules/*</exclude-pattern>
	<exclude-pattern>/mu-plugins/*</exclude-pattern>
	<exclude-pattern>/tests/*</exclude-pattern>
	<exclude-pattern>/logs/*</exclude-pattern>
	<exclude-pattern>/coverage/*</exclude-pattern>

	<!-- Show progress and sniff codes -->
	<arg value="ps"/>
	<arg name="colors"/>

	<!-- Check for PHP cross-version compatibility -->
	<config name="testVersion" value="8.2-"/>

	<!-- Use WordPress coding standards -->
	<rule ref="WordPress">
		<!-- Allow short array syntax -->
		<exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
	</rule>

	<!-- Use WordPress-Extra for additional checks -->
	<rule ref="WordPress-Extra"/>

	<!-- Use WordPress-Docs for documentation -->
	<rule ref="WordPress-Docs"/>

	<!-- Check text domain -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="bricks-mcp"/>
			</property>
		</properties>
	</rule>

	<!-- Verify function prefix -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="bricks_mcp"/>
				<element value="BricksMCP"/>
				<element value="BRICKS_MCP"/>
			</property>
		</properties>
	</rule>

	<!-- Allow PSR-4 file naming for classes -->
	<rule ref="WordPress.Files.FileName">
		<properties>
			<property name="strict_class_file_names" value="false"/>
		</properties>
		<exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
	</rule>
</ruleset>
